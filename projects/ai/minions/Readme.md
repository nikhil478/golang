# Minions

The idea behind minions is to create an isolated sandbox where an AI agent can perform its actions and store the resulting output within the same isolated container. The underlying intuition is similar to how a human uses a laptop: we provide the AI agent with its own self contained environment to work in